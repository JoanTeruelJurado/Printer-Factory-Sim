#!/bin/bash
# retailer-cli — wrapper that delegates to retailer_cli.py
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
source "$SCRIPT_DIR/venv/bin/activate" 2>/dev/null || true
exec python3 "$SCRIPT_DIR/retailer_cli.py" "$@"
